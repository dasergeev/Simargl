<local:NodeView x:Class="Apeiron.Platform.Demo.AdxlDemo.UserInterface.ChannelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Apeiron.Platform.Demo.AdxlDemo.UserInterface"
             xmlns:channels="clr-namespace:Apeiron.Platform.Demo.AdxlDemo.Channels"
                d:DataContext="{d:DesignInstance Type=channels:ChannelOrganizer}"
                mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="52"/>
            <RowDefinition/>
            <RowDefinition Height="52"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <GroupBox Header="Длительность фрагмента">
            <Slider Value="{Binding Duration}" Minimum="5" Maximum="60" Margin="5"/>
        </GroupBox>
        <GroupBox Header="Частота дискретизации" Grid.Column="1">
            <Slider Value="{Binding Sampling}" Minimum="1" Maximum="4000" Margin="5"/>
        </GroupBox>
        <GroupBox Header="Срез фильтра" Grid.Column="2">
            <Slider Value="{Binding Cutoff}" Minimum="1" Maximum="1000" Margin="5"/>
        </GroupBox>
        <local:Plotter x:Name="_ChannelPlotter" Grid.Row="1" Grid.ColumnSpan="3"/>
        <GroupBox Header="Время отображения" Grid.Row="2" Grid.ColumnSpan="3">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="110"/>
                </Grid.ColumnDefinitions>
                <DatePicker x:Name="_BeginTimePicker" SelectedDateFormat="Short" SelectedDateChanged="BeginTimePicker_SelectedDateChanged" />
                <Slider x:Name="_BeginTimeSlider" Grid.Column="1" VerticalAlignment="Center" ValueChanged="BeginTimeSlider_ValueChanged" SmallChange="0.000173611">
                    <Slider.ToolTip>
                        <ToolTip x:Name="_SliderToolTip" />
                    </Slider.ToolTip>
                </Slider>
                <CheckBox Grid.Column="2" Content="Ручной режим" IsChecked="{Binding IsCustomTime}" VerticalAlignment="Center"/>
            </Grid>
        </GroupBox>
    </Grid>
</local:NodeView>
