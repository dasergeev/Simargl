<local:NodeView x:Class="Apeiron.Platform.Demo.AdxlDemo.UserInterface.ChannelCollectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Apeiron.Platform.Demo.AdxlDemo.UserInterface"
             xmlns:channels="clr-namespace:Apeiron.Platform.Demo.AdxlDemo.Channels"
                d:DataContext="{d:DesignInstance Type=channels:ChannelOrganizerCollection}"
                mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="52"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="256"/>
        </Grid.ColumnDefinitions>
        <local:Plotter x:Name="_ChannelPlotter"/>
        <GroupBox Header="Время отображения" Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="110"/>
                </Grid.ColumnDefinitions>
                <DatePicker x:Name="_BeginTimePicker" SelectedDateFormat="Short" SelectedDateChanged="BeginTimePicker_SelectedDateChanged" />
                <Slider x:Name="_BeginTimeSlider" Grid.Column="1" VerticalAlignment="Center" ValueChanged="BeginTimeSlider_ValueChanged" SmallChange="0.000173611">
                    <Slider.ToolTip>
                        <ToolTip x:Name="_SliderToolTip" />
                    </Slider.ToolTip>
                </Slider>
                <CheckBox Grid.Column="2" Content="Ручной режим" IsChecked="{Binding IsCustomTime}" VerticalAlignment="Center"/>
            </Grid>
        </GroupBox>
        <ListView x:Name="_ChannelListView" Grid.Column="1" d:ItemsSource="{Binding Nodes}" Grid.RowSpan="2">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <CheckBox IsChecked="{Binding IsVisible}" VerticalAlignment="Center"/>
                        <Rectangle Width="16" Height="16" VerticalAlignment="Center" Margin="5,0,5,0">
                            <Rectangle.Fill>
                                <SolidColorBrush Color="{Binding MediaColor}"/>
                            </Rectangle.Fill>
                        </Rectangle>
                        <TextBlock Text="{Binding Name}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</local:NodeView>
