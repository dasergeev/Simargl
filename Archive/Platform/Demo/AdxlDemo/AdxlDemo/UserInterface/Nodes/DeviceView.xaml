<local:NodeView x:Class="Apeiron.Platform.Demo.AdxlDemo.UserInterface.DeviceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Apeiron.Platform.Demo.AdxlDemo.UserInterface"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ToolBar>
            <Button Click="Load">
                <Image Source="/Images/Repeat.png" Width="16" Height="16"/>
                <Button.ToolTip>
                    <ToolTip>
                        Загрузить текущие настройки из датчика. Обновить значения для редактирования.
                    </ToolTip>
                </Button.ToolTip>
            </Button>
            <Button Click="Save">
                <Image Source="/Images/Record.png" Width="16" Height="16"/>
                <Button.ToolTip>
                    <ToolTip>
                        Записать текущие настройки в датчик.
                    </ToolTip>
                </Button.ToolTip>
            </Button>
        </ToolBar>
        <ListView x:Name="_ListView" Grid.Row="1">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <GroupBox Header="{Binding Name}" HorizontalAlignment="Stretch">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Vertical">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Активное значение:"/>
                                    <TextBlock Text="{Binding ActiveValue}" Margin="5,0,5,0"/>
                                    <TextBlock Text="{Binding Unit}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Допустимый диапазон значений: от"/>
                                    <TextBlock Text="{Binding MinValue}" Margin="5,0,5,0"/>
                                    <TextBlock Text="{Binding Unit}"/>
                                    <TextBlock Text=" до"/>
                                    <TextBlock Text="{Binding MaxValue}" Margin="5,0,5,0"/>
                                    <TextBlock Text="{Binding Unit}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Оперативное значение:" VerticalAlignment="Center"/>
                                    <ComboBox MinWidth="128" VerticalAlignment="Center" Margin="5,0,0,0" SelectedIndex="{Binding ValueIndex}" ItemsSource="{Binding TextValues}" Visibility="{Binding NotContinuousVisibility}" />
                                    <TextBox MinWidth="128" VerticalAlignment="Center" Margin="5,0,0,0" Text="{Binding TextValue}" Visibility="{Binding ContinuousVisibility}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <Slider MinWidth="256" Value="{Binding Value}" Minimum="{Binding MinValue}" Maximum="{Binding MaxValue}" Visibility="{Binding ContinuousVisibility}" IsSnapToTickEnabled="True" Ticks="{Binding TickMarks}"/>
                                </StackPanel>
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{Binding Description}"/>
                        </Grid>
                    </GroupBox>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</local:NodeView>
