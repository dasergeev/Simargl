<Window x:Class="Apeiron.Oriole.Analysis.UserInterface.AnalyzerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Apeiron.Oriole.Analysis.UserInterface"
        xmlns:input="clr-namespace:System.Windows.Input;assembly=netstandard"
        mc:Ignorable="d"
        Title="Анализатор исходных данных" Height="450" Width="800">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="booleanToVisibilityConverter" />
    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding Command="New" Executed="New_Executed" CanExecute="New_CanExecute"/>
        <CommandBinding Command="Open" Executed="Open_Executed" CanExecute="Open_CanExecute"/>
        <CommandBinding Command="Save" Executed="Save_Executed" CanExecute="Save_CanExecute"/>
    </Window.CommandBindings>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="28"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ToolBar Grid.ColumnSpan="2">
            <Button Command="New" ToolTip="Создать новый проект."
                    CommandParameter="{Binding Path=PlacementTarget.DataContext, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContextMenu}}}"
                    CommandTarget="{Binding Path=PlacementTarget, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContextMenu}}}"
                    Visibility="{Binding Path=IsEnabled, RelativeSource={RelativeSource Self}, Mode=OneWay , Converter={StaticResource booleanToVisibilityConverter}}">
                <Image Source="pack://application:,,,/Resources/Images/NewProject.png"/>
            </Button>
            <Button Command="Open" ToolTip="Открыть проект."
                    CommandParameter="{Binding Path=PlacementTarget.DataContext, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContextMenu}}}"
                    CommandTarget="{Binding Path=PlacementTarget, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContextMenu}}}"
                    Visibility="{Binding Path=IsEnabled, RelativeSource={RelativeSource Self}, Mode=OneWay , Converter={StaticResource booleanToVisibilityConverter}}">
                <Image Source="pack://application:,,,/Resources/Images/OpenProject.png"/>
            </Button>
            <Button Command="Save" ToolTip="Сохранить проект."
                    CommandParameter="{Binding Path=PlacementTarget.DataContext, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContextMenu}}}"
                    CommandTarget="{Binding Path=PlacementTarget, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContextMenu}}}"
                    Visibility="{Binding Path=IsEnabled, RelativeSource={RelativeSource Self}, Mode=OneWay , Converter={StaticResource booleanToVisibilityConverter}}">
                <Image Source="pack://application:,,,/Resources/Images/SaveProject.png"/>
            </Button>
        </ToolBar>
        <local:ProjectView x:Name="_ProjectView" Grid.Row="1"/>
        <GridSplitter x:Name="gridSplitter" Grid.Column="1" HorizontalAlignment="Left" Margin="0,0,0,0" Grid.Row="1" Width="5"/>
        <local:NodeView x:Name="_NodeView" Grid.Column="1" Margin="5,0,0,0" Grid.Row="1"/>
    </Grid>
</Window>
