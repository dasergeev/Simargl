<local:NodeView x:Class="Apeiron.Platform.Demo.AdxlDemo.UserInterface.DeviceCollectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Apeiron.Platform.Demo.AdxlDemo.UserInterface"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <GroupBox Header="Датчики" >
            <ListView x:Name="_DeviceListView">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Label Tag="{Binding SerialNumber}" MinHeight="64" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Background="LightGray" Margin="10" MouseDown="Label_MouseDown">
                            <StackPanel Orientation="Vertical">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Серийный номер: " TextAlignment="Center" HorizontalAlignment="Stretch"/>
                                    <TextBlock Text="{Binding Name}" TextAlignment="Center" HorizontalAlignment="Stretch"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Частота дискретизации: " TextAlignment="Center" HorizontalAlignment="Stretch"/>
                                    <TextBlock Text="{Binding Sampling}" TextAlignment="Center" HorizontalAlignment="Stretch"/>
                                    <TextBlock Text=" Гц" TextAlignment="Center" HorizontalAlignment="Stretch"/>
                                </StackPanel>
                            </StackPanel>
                        </Label>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </GroupBox>
        <GroupBox Header="Каналы" Grid.Column="1">
            <ListView x:Name="_ChannelListView">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <GroupBox Tag="{Binding Number}" AllowDrop="True" Drop="Number_Drop">
                            <GroupBox.Header>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Группа каналов №"/>
                                    <TextBlock Text="{Binding Number}"/>
                                </StackPanel>
                            </GroupBox.Header>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Label MinHeight="64" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Background="LightGray" Margin="10">
                                    <StackPanel Orientation="Vertical">
                                        <TextBlock Text="Текущий датчик"/>
                                        <TextBlock Text="{Binding SerialNumber, StringFormat=X8}" TextAlignment="Center"  HorizontalAlignment="Stretch"/>
                                    </StackPanel>
                                </Label>
                                <ListView Grid.Column="1" ItemsSource="{Binding Channels}">
                                    <ListView.View>
                                        <GridView>
                                            <GridViewColumn Header="Имя"  DisplayMemberBinding="{Binding Name}"/>
                                            <GridViewColumn Header="Тип"  DisplayMemberBinding="{Binding ChannelType}"/>
                                            <GridViewColumn Header="Сигнал"  DisplayMemberBinding="{Binding SignalNumber}"/>
                                            <GridViewColumn Header="Частота"  DisplayMemberBinding="{Binding Sampling}"/>
                                            <GridViewColumn Header="Фильтр"  DisplayMemberBinding="{Binding Cutoff}"/>
                                        </GridView>
                                    </ListView.View>
                                </ListView>
                            </Grid>
                        </GroupBox>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </GroupBox>
    </Grid>
</local:NodeView>
