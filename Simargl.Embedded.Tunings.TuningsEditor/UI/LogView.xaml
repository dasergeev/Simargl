<UserControl
    x:Class="Simargl.Embedded.Tunings.TuningsEditor.UI.LogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:core="clr-namespace:Simargl.Embedded.Tunings.TuningsEditor.Core"
    mc:Ignorable="d" 
    d:DesignHeight="450" d:DesignWidth="800"
    d:DataContext="{d:DesignInstance Type=core:Heart}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ToolBar ToolBarTray.IsLocked="True">
            <Button Command="{Binding StartLogCommand}">
                <Image Source="/Images/Data/Start.png" Width="16" Height="16" />
                <Button.ToolTip>
                    <StackPanel>
                        <TextBlock Text="Запустить" FontWeight="Bold"/>
                        <TextBlock Text="Запустить работу журнала">
                            <TextBlock.LayoutTransform>
                                <ScaleTransform ScaleX="0.8" ScaleY="0.8"/>
                            </TextBlock.LayoutTransform>
                        </TextBlock>
                    </StackPanel>
                </Button.ToolTip>
            </Button>
            <Button Command="{Binding StopLogCommand}">
                <Image Source="/Images/Data/Stop.png" Width="16" Height="16" />
                <Button.ToolTip>
                    <StackPanel>
                        <TextBlock Text="Остановить" FontWeight="Bold"/>
                        <TextBlock Text="Остановить работу журнала">
                            <TextBlock.LayoutTransform>
                                <ScaleTransform ScaleX="0.8" ScaleY="0.8"/>
                            </TextBlock.LayoutTransform>
                        </TextBlock>
                    </StackPanel>
                </Button.ToolTip>
            </Button>
            <Separator/>
            <Button Command="{Binding ClearLogCommand}">
                <Image Source="/Images/Data/New.png" Width="16" Height="16" />
                <Button.ToolTip>
                    <StackPanel>
                        <TextBlock Text="Очистить" FontWeight="Bold"/>
                        <TextBlock Text="Очистить журнал">
                            <TextBlock.LayoutTransform>
                                <ScaleTransform ScaleX="0.8" ScaleY="0.8"/>
                            </TextBlock.LayoutTransform>
                        </TextBlock>
                    </StackPanel>
                </Button.ToolTip>
            </Button>
        </ToolBar>
        <ScrollViewer
            Grid.Row="1"
            VerticalScrollBarVisibility="Visible"
            HorizontalScrollBarVisibility="Visible">
            <TextBox
                x:Name="LogBox"
                Text="{Binding LogText, Mode=OneWay}"
                FontFamily="Consolas"
                FontSize="12"
                IsReadOnly="True"
                AcceptsReturn="True"
                TextWrapping="NoWrap"/>
        </ScrollViewer>
    </Grid>
</UserControl>
