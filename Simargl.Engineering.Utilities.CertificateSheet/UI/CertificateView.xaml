<UserControl
    x:Class="Simargl.Engineering.Utilities.CertificateSheet.UI.CertificateView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:Simargl.Engineering.Utilities.CertificateSheet.UI"
    xmlns:core="clr-namespace:Simargl.Engineering.Utilities.CertificateSheet.Core"
    d:DataContext="{d:DesignInstance Type=core:Certificate}"
    mc:Ignorable="d" 
    d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding FileName}"/>
            <TextBlock Grid.Column="1" Text="Найдено файлов: "/>
            <TextBlock Grid.Column="2" Text="{Binding CountFiles}"/>
        </Grid>
        <TextBlock Grid.Row="1" Visibility="{Binding ExceptionVisibility}" Text="{Binding ExceptionMessage}"/>
    </Grid>
</UserControl>
